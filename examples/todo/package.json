{
  "name": "todo",
  "private": true,
  "description": "Server-authoritative realtime Todo app built with Beryl and Lustre.",
  "type": "module",
  "scripts": {
    "build": "cd client && gleam run -m lustre/dev build",
    "dev": "pnpm build && gleam run",
    "start": "gleam run",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "dependencies": {
    "phoenix": "^1.8.11"
  }
}
