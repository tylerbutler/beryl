# Ignore everything by default; the Dockerfile copies the whole repo so we
# whitelist only the bits the build needs. Run `docker build` from the
# repository root with `-f examples/showcase/Dockerfile .`.
**/build/
**/_build/
**/node_modules/
**/.git/
**/test-results/
**/playwright-report/
*.log
erl_crash.dump
