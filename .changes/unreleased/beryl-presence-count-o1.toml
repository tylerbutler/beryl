project = "beryl"
kind = "Fixed"
body = "presence.count is now genuinely O(1): the read model stores each topic's entry count alongside its entries in ETS, so count fetches only the count field via ets:lookup_element/4 instead of materializing and measuring the entry list."
