package = "beryl"
kind = "Added"
body = "BREAKING: transport.admit_socket takes a new codec argument, letting one runtime serve connections with different wire formats while sharing topics and PubSub. Passing None uses the configured codec."
