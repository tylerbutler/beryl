project = "beryl"
kind = "Added"
body = "Phoenix V2 wire protocol (JSON text framing and the binary subprotocol) as the default codec, plus a pluggable codec API (beryl/wire/codec) for custom text/binary framing with opt-in topicless events and close/error encoders."
