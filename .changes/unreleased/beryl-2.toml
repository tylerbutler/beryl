package = "beryl"
kind = "Fixed"
body = "Corrected public API documentation: the real max_topic_length behavior, standardized wildcard terminology, and the Sender-based beryl/bridge example. Presence debug logs now use the session_id metadata key instead of pid."
