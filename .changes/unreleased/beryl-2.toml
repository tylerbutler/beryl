project = "beryl"
kind = "Fixed"
body = "Corrected stale and false documentation across the public API: removed references to deleted startup and channel-module APIs, documented the real max_topic_length behavior, restored the Sender-based beryl/bridge example, and standardized wildcard terminology. Presence debug logs now use the session_id metadata key instead of pid."
