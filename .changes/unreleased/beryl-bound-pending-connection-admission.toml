package = "beryl"
kind = "Added"
body = "Bound pending connection admission and cancellation with configurable count and payload limits, reject saturation before mailbox publication, reclaim unacknowledged grants across limiter restarts, recover cancelled holders if a releasing caller dies before notification, and safely cancel permits when the limiter is unavailable."
