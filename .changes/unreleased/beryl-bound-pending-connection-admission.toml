package = "beryl"
kind = "Added"
body = "Bound pending connection admission and cancellation with configurable count and payload limits, reject saturation before mailbox publication, and reclaim unacknowledged grants across limiter restarts."
